name: hududrun
description: "HududRun — yugurish orqali hududlarni egallang va raqobatlashing."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  shared_preferences: ^2.3.3
  http: ^1.6.0
  flutter_secure_storage: ^9.0.0
  google_fonts: ^6.2.1
  cached_network_image: ^3.4.1
  percent_indicator: ^4.2.3
  shimmer: ^3.0.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  geolocator: ^12.0.0
  flutter_map_cancellable_tile_provider: ^2.0.0
  image_picker: ^1.1.2
  url_launcher: ^6.3.1
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  generate: false
  uses-material-design: true
